{
  "mcpServers": {
    "localci": {
      "type": "stdio",
      "command": "nix",
      "args": ["run", "--refresh", "github:srid/localci/mcp-integration", "--", "--mcp", "--sha", "HEAD", "-f", "localci.json"]
    }
  }
}
